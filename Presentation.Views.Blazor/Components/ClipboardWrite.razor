@inherits ModelComponentBase<String>

<span @attributes="Attributes" @onclick="()=>Clipboard.WriteTextAsync(Model)">(copy)</span>

@code {
    [Injected] public required IClipboardModel Clipboard { get; set; }
}